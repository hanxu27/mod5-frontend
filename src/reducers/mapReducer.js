let initialState = {
  latitude: 38.560954,
  longitude: -98.935240,
  zoom: 4,
  width: '100vw',
  height: '100vh',
  selected: null
}

export default (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state
    }
  }
}